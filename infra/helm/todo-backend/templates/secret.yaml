{{- /*
  NOTE: This Secret template uses placeholder values that MUST be overridden at install time.
  Do NOT commit real secrets. Create the secret manually before Helm install:
    kubectl create secret generic todo-secrets \
      --from-literal=NEON_DB_URL='<value>' \
      --from-literal=BETTER_AUTH_SECRET='<value>' \
      --from-literal=COHERE_API_KEY='<value>'
  Then deploy with: helm install todo-backend infra/helm/todo-backend
  The deployment.yaml references the pre-created secret; this template is NOT rendered.
*/ -}}
